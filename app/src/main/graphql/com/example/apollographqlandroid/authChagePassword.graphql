mutation authChagePassword($token:String!,$password:String!,$newpassword:String!){
    authChagePassword(authChangePass:{
        Token: $token
        Password: $password
        NewPassword: $newpassword
    })
}
